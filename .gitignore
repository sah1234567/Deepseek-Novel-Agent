# ── Rust / Cargo ─────────────────────────────────────────────
/target/
**/target/
lcov.info
**/lcov.info

# Local CI / agent scratch logs (not part of the product)
.ci-*.txt

# ── Node / 前端 ──────────────────────────────────────────────
ui/node_modules/
ui/dist/
*.tsbuildinfo

# ── Tauri 生成文件 ────────────────────────────────────────────
src-tauri/gen/schemas/

# ── 用户作品与本地状态（勿提交）──────────────────────────────
works/*
!works/.gitkeep

# API Key 与本地 Agent 配置（用 env 或 Settings 自行配置）
.novel-agent/api_config.json

# 章节版本化快照（REGATE 对比用，非产物）
**/knowledge/meta/versions/

# ── 环境 / 日志 ──────────────────────────────────────────────
.env
.env.*
*.log

# ── OS / IDE ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
